<!-- GENERATED FROM docs/backlog.yml by hello-world/scripts/backlog/render.mjs — DO NOT HAND-EDIT -->

# Backlog

The queue of work this repo owes: **what is owed, by whom, and measured how.**

Read this **before starting anything** (step 0 of the dev loop). An item that lives only in a scratchpad, a
subagent report, or a chunk's own criteria is a deletion with extra steps — nothing reads those at the start of
the next piece of work.

## Rules for this file

1. **Every quantity names its command or `file:line`.** A number without an instrument rots silently, and this
   repo has already shipped counts that were wrong in three documents at once.
2. **Record at disposal, reconcile at the push.** When a check or a round disposes of a finding as "later",
   append it here *then* — not at the end. Residuals are created many waves before a push.
3. **No diary.** This is not a log of what was done. Closed items are deleted, not archived. Use `git log` for
   history.
4. **Owner-only items are never started by an agent.** They are listed so they are not forgotten, not so they
   are picked up.
5. **The loop does not stop while this file has entries.** Finishing a chunk is not finishing the work — the
   next action is always the next backlog item. See "How the loop consumes this file" below.

## How the loop consumes this file

**While this file is non-empty, the dev loop does not stop.** At the end of every chunk, wave, or ruling, the
next action is read off this file rather than chosen freshly.

| Section | What the loop does |
|---|---|
| **Next — actionable now** | Work it. Top to bottom unless a dependency says otherwise. This is where the loop spends its time. |
| **Owner decisions outstanding** | **Never started.** When these are all that remain, the loop's action is to **put them to the owner as a decision** — once, with what is blocked and what would unblock it. Escalating is a step; idling is not. |
| **Verification owed** | Same: surface it, with the exact instrument the owner would run. |

**The loop ends a turn only after it has either advanced an actionable item or escalated a blocked one.**
Stopping with actionable entries present and neither done is the failure this rule exists to prevent.

### Disjoint items are worked SIMULTANEOUSLY

The loop does not work the backlog one item at a time when items do not touch each other. Before dispatching,
compute each candidate item's **file set** and run them together when the sets do not intersect.

**Disjointness has two halves, and both must hold. File-disjoint alone is not enough** — that lesson cost this
repo a whole contract mechanism when two design seats with non-overlapping outputs produced two incompatible
schemas for the same table, because each was designing against facts the other was still establishing.

1. **Exact-path disjointness — computed, never eyeballed.** An item's set is *the files it edits* **plus the
   tests that assert on the behaviour it changes**. Intersect the sets mechanically (`sort | uniq -d`, empty
   output) and paste the result.
2. **Informational independence.** Ask of each pair: *does either establish a fact the other designs against?*
   If yes they are coupled however disjoint their files are — sequence them, or extract the shared contract
   into its own earlier step, alone.

**Cap a simultaneous wave at 2–3 items.** A larger fan-out has produced duplicated discovery here — three
agents independently finding the same blocker, and one designing a solution to a problem a sibling was
concurrently proving did not exist.

**Worked example, run 2026-09-13 — and it refuted the obvious answer.** `SEC-1` (item 1) looked like the ideal
parallel candidate: a one-file resolver fix, unrelated to interview-prep. Computed:
`grep -rln "featureEngine\|wantsEmbedded" --include=*.js app lib` → **58 files** (canary
`zzNoSuchSymbolzz` → 0), and the set **includes `app/api/interview-prep/route.test.js` and
`lib/copilot/groundingNotice.js`** — files IP3 and IP-N own. **Not disjoint.** SEC-1 waits.
Items 2 and 3 (`scripts/regression/**` and a regression AC ruling) *are* disjoint from IP3's
`lib/interviewPrep/**`, and run alongside it.

**A blocked item is escalated once, not repeatedly.** Re-asking the same unanswerable question every turn is
spinning wearing a decision's clothes. Once surfaced, it stays listed and silent until the owner answers or the
blocker clears.

---

## Next — actionable now

| # | Item | Owed by | Measured how |
|---|---|---|---|
| N2 | **O-2 step 7 precedence is ambiguous** when both the `Test Files` and `Errors` lines hold. No fix round is scheduled. | A regression AC round | ledger `T3-C4b-6`, `T3-V6-6` |
| N3 | **Three 4b instrument minors, disclosed and unfixed.** `C4B-6` an import-extension regex fragility; `C4B-7` the reason-vocabulary CHECK canary drawn from the same source it validates (partially mitigated); `C4B-8` an outcome-check that asserts containment only | A later 4b round | `check-4b.r1.md` majors table; scoped out by `4b.r2.md` |
| N4 | **The K1-PROHIBITION corpus is held-out by instruction, not by construction.** `lib/interviewPrep/__fixtures__/predictionCorpus.js` sits in the implementer's own module directory; nothing prevents reading it. Proven discriminating today: a 10-string lookup-table build passes all 27 visible rows and ships both held-out sentences unrefused | A later hardening round — derive held-out members rather than store them | `4b.r2.md`; ruling `R-IP3-64` |
| N5 | **Three named contract functions have no test and no implementation.** `packRenderState`, `packIsStale`, `shouldStartPrep` are named by `design-structure.r1.md` §3 but no 4b row exercises them, so step-5 wave 1 correctly declined to build them against a guessed row shape. A coverage gap in 4b, not an implementation gap | A 4b round, then the wave that owns them | `prepContract.js`'s own comment names the gap; step-5 wave 1 report |
| N6 | **`launch.js` L7's `lockedIds` guard is unswept** — no faithful dangerous mutant was found for it. Named, not asserted clean | A later sabotage pass | `sabotage.r2.md` |
| N7 | **`prepLog.js` was built with no specifying document and no test.** 86 lines, created because the wave table needs the file to exist for a later mechanical gate and the UI wave. Its export shape is invented, and nothing exercises it — the same class wave 1 correctly refused for three other functions | A 4b round before it carries behaviour | `lib/interviewPrep/prepLog.js` header discloses it; step-5 wave 2 report |
| N8 | **The prep prompt does NOT send the résumé or cover letter.** `route.js` writes `resumeId`/`coverLetterId` as `null` and generates from the posting text plus the digest only, because no binding document names a résumé/cover-letter store. **Two consequences**: answers are not tailored to the candidate's actual material, which is the feature's point; and **O-12's disclosure says the résumé and cover letter are sent**, so the notice currently over-states what leaves the app | A design round to name the store, then the wave that wires it — **and IP-N re-checks the notice text against what is actually sent** | `app/api/interview-prep/route.js` header discloses it; owner ruling **O-12** |
| N11 | **O-7's digest-ensure forwards the caller's cookie** in a server-to-server fetch, because no existing helper does this. Security-relevant and unreviewed | A security review round | disclosed in `route.js`'s header |
| N13 | **Two of the five B1 trigger sites are inert.** `generateWithReviewedValues` (`app/page.js`) and `runWorker` (`app/hooks/useManualPostings.js`) call `startInterviewPrepResearch` with **no resolvable `applicationId`** — `tailorPosting`'s return carries only `jobId` — so `prepTrigger.js`'s blank-id refusal fires and nothing happens downstream. The landed seam tests assert the *call*, which is made, so they stay green. **Three of five manual-tailor paths work; two do not.** | A wave that threads the application id through the tailor return, or a design round that decides it cannot be | `plan.r1.md` §4 names this as an unresolved tracing gap; the step-5 waves 8/9 report documents both call sites and the reliance on the blank-id refusal |
| N15 | **A tracking row cannot open the preview/edit window.** Owner request, 2026-09-14. `app/components/tracking/ApplicationCard.js` gives each row `JD` (`:178`) and `Resume` (`:181`) buttons, but both open `AppViewDialog` with a `kind`, which is a **read-only view** — not `DocumentPreviewDialog`, the window that carries editing, the rich-copy path, filename control, download and Save-to-Drive. That dialog is mounted once by `app/components/DocumentPreviewMount.js` and driven by the `preview` state of the main tailoring flow, so it is reachable only by tailoring a posting in this session: a row tailored on a previous visit has no route to it at all. **What is owed**: a per-row action on the tracking table that opens `DocumentPreviewDialog` for that row's tailored résumé and cover letter. **Two things a design round must settle before code**: (1) the dialog is a single long-lived mount reading one `preview` object — opening it for an arbitrary row means either populating that state from the row or giving the mount a second entry point, and which one is correct is not derivable from the current code; (2) the stored-document source for a row is the same unnamed résumé/cover-letter store that **N8** is blocked on, so if the row's documents are not already in hand this item inherits that blocker. **Constraint**: the standing minimize-clicks directive applies — one action with good defaults, not a menu then a picker | A design round settling the mount's second entry point and the row→document source, then the wave that builds it | `app/components/tracking/ApplicationCard.js:178`, `:181` (the two existing read-only row buttons); `app/components/DocumentPreviewMount.js` (single mount, driven by the tailoring flow's `preview` state); `app/components/DocumentPreviewDialog.js` (the window owed); coupled to **N8** for the document source |
| N17 | **Voice enrollment: identify the candidate's own voice from an uploaded sample of them reading an app-provided passage.** Owner request, 2026-09-14. **Scope is narrower than it sounds, and the survey is why**: for a REMOTE interview the app already separates the two voices by CHANNEL — `lib/copilot/capture.js` takes the candidate's own voice from `getUserMedia` (mic) and the other party's from `getDisplayMedia` (tab/system audio), so enrollment adds nothing there. The case that actually needs this is the **SHARED MICROPHONE / in-person** session, which is exactly what `lib/copilot/speakerIdentity.js` exists for today — and it identifies the candidate BEHAVIOURALLY (word share, question detection, confidence gates), not by voice. **The motivating failure is already documented in that file's header**: v1 scored on word share alone and elected the INTERVIEWER as the user at 'high' confidence on two real interview openings, after which the copilot stopped evaluating their speech for questions and went **silently deaf for the rest of the session** — the file calls this the worst failure mode the feature has, because the user cannot tell it happened. A voiceprint replaces a heuristic that is known to fail silently, which is the strongest case for building it. **THE FEASIBILITY CRUX, unresolved**: diarization as wired today (`lib/copilot/stt/index.js`, Deepgram and ElevenLabs Scribe) yields ANONYMOUS speaker tags (`speakerTag` = provider-assigned id), never an identity — binding tag→person needs either provider-side speaker enrollment/verification or a locally-run speaker-embedding model, and NEITHER is confirmed available on the current providers. That question must be settled at primary source BEFORE any UI is designed, or the enrollment flow gets built against a capability that does not exist. **Why an app-provided passage is the right call** (the owner asked for it and it is correct): a known transcript gives phonetic coverage, lets the app verify the upload is actually someone reading THAT passage rather than arbitrary audio, and supports a quality gate (too short, too noisy, clipped) before a bad enrollment silently degrades every later session. **A voiceprint is BIOMETRIC data** — distinct from the recordings the copilot already stores — **OWNER RULING, 2026-09-14: storing the candidate's voice is PERMITTED, provided they are made aware of it and consent.** That settles whether the feature may exist. Three things it does NOT settle, and a design round must: (a) what deletion looks like — consent to store is not consent to keep forever, and the copilot already ships a delete control precedent (O-16's per-application delete) rather than a tombstone; (b) whether the consent is one-time at enrollment or re-surfaced, and where the disclosure actually appears — this repo has already shipped a disclosure that OVER-STATED what left the app (see N8), so the notice text must be checked against what is actually sent; (c) **conditional, and only if the research round finds provider-side enrollment is the ONLY feasible path**: consent to the APP storing a voiceprint is not obviously consent to transmitting it to a third-party STT vendor, which is a separate egress the owner should rule on once it is known to be necessary | A 3b research round settling provider speaker-ID/enrollment support at primary source and an owner ruling on biometric storage, THEN a design round, THEN the wave that builds it | `lib/copilot/speakerIdentity.js` header (the behavioural identifier this replaces, and its silently-deaf failure); `lib/copilot/capture.js:4-12` (channel separation already solves the remote case); `lib/copilot/stt/index.js:117-137` (`speakerTag` is provider-assigned and anonymous; `diarizationActive`); `lib/copilot/answerSpeakers.js` header (the no-tag-means-candidate compatibility rule any change here must preserve) |
| N19 | **`lib/duplicateApply/`'s purity guard cannot see the reintroduction it exists to forbid.** `duplicateApplyVerdict.js:16-17` states the guarantee — *no I/O, no network call, no ambient clock read* — and the feature is shipped and user-facing (`app/components/StatusBar.js`, `app/hooks/useDuplicateApplyCheck.js`). The guard at `duplicateApplyPurity.test.js:82-86` pins `LLM_DIR_RE = /lib\/llm/`, which matches only the `@/` alias spelling, while `SUPABASE_RE` on the very next line matches both spellings. **Measured by transcribing the guard's own regexes and its `stripComments()` into a probe**: the planted control `@/lib/llm/gemini.js` is caught, but `import { getGeminiClient } from "../llm/geminiClient.js"` **passes every assertion**, as do a dynamic relative import, `node:https`, `XMLHttpRequest` and `navigator.sendBeacon`. `../llm/…` is not hypothetical — it is live convention here (`lib/meeting/referenceContract.js:21`, `lib/techwatch/lifecycleSearch.js:15`). **Same alias-only hole in four more guards**: `lib/copilot/glossaryMatch.test.js:314`, `lib/copilot/expansionStore.test.js:178`, `app/hooks/useMaterialsLocker.extraction.test.js:71`, `app/components/windowOpenSafety.sweep.test.js:538`. `glossaryMatch` survives only by accident, because `:318` also asserts the module imports nothing at all — **and that is the fix model**. **The fix is an import ALLOWLIST, not a forbidden-substring denylist**, and it is cheap: 5 of 6 shipped `duplicateApply` modules have zero imports and `duplicateApplyVerdict.js` has four. Also drop the `isDirectory() continue` at `:69`, whose comment asserts a fact the walker should measure | A hardening round — replace the denylist with an allowlist across all five guards | `lib/duplicateApply/duplicateApplyPurity.test.js:82-86`, `:69`, `:137-145`; `lib/duplicateApply/duplicateApplyVerdict.js:16-17`; `lib/meeting/referenceContract.js:21`, `lib/techwatch/lifecycleSearch.js:15` (the live `../llm/` convention); `lib/copilot/glossaryMatch.test.js:314` and `:318` (the hole, and the allowlist shape that saves it) |
| N20 | **`lib/llm/engines/tailor-lite/` has no purity sweep, though it is the app's largest no-model-call guarantee.** `wantsEmbedded` governs the embedded path on 20 routes and tailor-lite (55 files) is the deterministic backend the whole no-egress claim rests on. **Two instruments, both negative**: `grep -rln "tailor-lite" --include=*.test.js` → 6 files, none a purity sweep (`injection.test.js` is a DI test); and no `readFileSync`/`readdirSync` appears in any test under `lib/llm/`, which a source-text sweep would require. Canary `zzNoSuchSymbolzz` → 0. tailor-lite is **clean today** (grep for `@google/genai|lib/llm/gemini|fetch(` over its non-test sources → empty) — nothing keeps it clean. **Fix**: one sweep modelled on `duplicateApplyPurity.test.js` but allowlist-shaped (see N19), with the planted positive control that file already gets right. **Instrument limit**: the survey checked static import text, not transitive reachability | The same hardening round as N19 | `lib/llm/engines/tailor-lite/` (55 files, no sweep); `lib/llm/featureEngine.js` `wantsEmbedded`; `lib/duplicateApply/duplicateApplyPurity.test.js` (the shape to copy, once N19 fixes it) |
| N21 | **The backlog contract never checks that a path named in `verify` exists, and vitest silently absorbs one that does not.** **Measured on vitest 4.1.8**: `npx vitest run lib/sourceScan/stripSqlComments.test.js lib/sourceScan/zzNoSuchFilezz.test.js` → `Test Files 1 passed (1)`, **exit 0** — the missing path is absorbed. The control, that same missing path alone, exits 1 with `No test files found`. Positionals are substring FILTERS, not paths, so a union of them hides every miss behind one hit. `scripts/backlog/lib/contract.mjs` imports only `node:crypto`, never touches the filesystem, and nothing in the repo executes a `verify` string (`sessionStartText.mjs:12` only prints it). **Dormant only in data**: all current items carry `verify: null`, so the contract reports 0 violations. Both hazards fire on the first item anyone scopes — which is exactly the phase-2 work the schema header describes. **Fix**: inside `contract.mjs`'s existing `if (item.verify != null)` branch, split the command on whitespace and `existsSync` every token matching a test-file extension against the already-exported `HELLO_WORLD_ROOT`, violating with a message that names the absorbed path. **Related, minor**: `contract.test.js:157` calls it "the migrated 15-item file" when it holds 16 — stale title only, assertions correct | A tooling round, before any item is scoped with a real `verify` | `scripts/backlog/lib/contract.mjs` (no filesystem access; `verify_proof` exit codes are hand-recorded and unfalsifiable by construction, `:22-25`); `scripts/backlog/lib/loadBacklog.mjs:8` (`HELLO_WORLD_ROOT`, already exported); measured on vitest 4.1.8, command and control above |
| N23 | **Two whole-tree scanning tests sit at their 5000ms default timeout and flake under concurrent load, producing FALSE regressions on the next gate anyone runs.** Measured twice on 2026-09-15, both under load from concurrent work, both green in isolation: `lib/copilot/answerCarriers.test.js` ("AC-B.17 ... is emitted from exactly ONE site in the tree") failed at **9094ms** during a full-suite run and passed in **995ms** run alone; `lib/sourceScan/tokenizeSource.test.js` ("tokenises every .js file") measures **4.85s in isolation** against the 5000ms default - i.e. it has ~150ms of headroom before it fails for a reason unrelated to what it asserts. Both walk the whole source tree, so their runtime scales with the repo and is already at the limit. **Why this matters beyond the annoyance**: this repo gates pushes on a full-suite run, and a red that is really a stopwatch teaches the next reader to re-run rather than investigate - which is the habit that lets a REAL red be waved through. It also cost real time today: one full-suite failure was investigated as a regression before being identified as a timeout. **Fix**: give each an explicit timeout matched to its measured cost with headroom (not a blanket raise), or cache the tree scan across the suite. Whichever is chosen, record the MEASURED runtime beside the constant so the next person can tell drift from load | A test-infrastructure round | `lib/copilot/answerCarriers.test.js` - 9094ms under load, 995ms in isolation (both observed 2026-09-15); `lib/sourceScan/tokenizeSource.test.js` - 4.85s measured in isolation against a 5000ms default; both are whole-tree walkers, so runtime grows with the repo |
| N24 | **The live interview panel is too small to be useful on non-mobile screens.** Owner report, 2026-09-15. The copilot dashboard is the surface a candidate reads WHILE being interviewed, and on a laptop or desktop it wastes most of the window. **Grounded in the constraints that produce it, not a vibe**: the sticky question band is capped at **30% of the viewport height** - `BAND_BASE_DVH` in `app/copilot/stickyQuestionGuards.test.js:48` pins `max(calc(64px + 4.25rem), calc((100dvh - var(--sticky-top, 0px)) * 0.30))` - so on a 1080p screen the question and its answer share roughly a third of the height while two thirds sit empty. The voice-cue rail then takes a fixed **280px** at `md` and up (`app/copilot/VoiceCueSidebar.js:30` `RAIL_SX`), which is a far larger proportional bite at 1280px than the layout was tuned for. The column arithmetic (`CopilotClient.js:862`, `:873`, `:904`) is a `flex: 1 / minHeight: 0` chain written so the pieces fit on a phone, and it holds the same shape all the way up. **Almost every sizing decision in this surface was made for the phone case and never re-judged for the desktop one** - see [[copilot-mobile]], which records the mobile pass; there has been no desktop equivalent. **What this must NOT become**: more panels visible at once. The candidate is reading this while a person waits for them to speak, so the goal is BIGGER TYPE AND FEWER THINGS, not denser information - the [[minimize-clicks]] reasoning applied to attention rather than clicks. **A design round should settle**: what the desktop breakpoint gives back to the answer area; whether the rail collapses, docks or moves at `lg`+; whether the band cap should be a function of available height rather than a fixed fraction; and what the reading distance actually is (a laptop at arm length is not a phone at 30cm, and the type scale never changed). **Measurement limit, stated honestly**: this repo cannot measure real layout - jsdom computes no geometry and the browser pane needs a visible window ([[measurement-instruments]]). The design round needs real screenshots at real breakpoints, which is an owner-run instrument | A design round (1c) for the desktop breakpoint, then the wave that builds it | `app/copilot/stickyQuestionGuards.test.js:48` (`BAND_BASE_DVH` - the 30%-of-viewport cap); `app/copilot/VoiceCueSidebar.js:30` (`RAIL_SX` - fixed 280px at md+); `app/copilot/CopilotClient.js:862`, `:873`, `:904` (the flex/minHeight column chain); [[copilot-mobile]] records the mobile pass; no desktop equivalent exists |
| N25 | **The interview prep pack has NO READ SURFACE - nothing in the app ever shows a candidate their pack.** Found by the N16/N8 design round, 2026-09-15, and it reframes what "fixing the prep pack" means. **Measured, with a canary**: `readPrepPack` and `listPrepPacks` (`lib/interviewPrep/prepStore.js`) have ZERO non-test callers; there is no `GET` handler in `app/api/interview-prep/` (the directory holds only `route.js` and `route.test.js`); and no component renders a pack. The canary on the same instrument (`fetchApplicationDocs`) returned 40+ hits across routes and libs, so the absence is real and not a dead grep. **Consequence, and it must be said out loud when N16/N8 land**: those two items make the pack GENERATE correctly and LAND in the database grounded in the candidate materials - and the candidate still sees nothing. `prepTrigger.js:51-74` fires the POST and discards the response. Reporting N16/N8 as "the prep pack now works" would be false. **What this wave owes**: a `GET` on the prep route (or a direct client read - `prepStore.js`’s reads are deliberately browser-safe, its own header says so), a component, and the rendered disclosure notice `lib/interviewPrep/prepNotice.js` produces. **One rule the design round established that this wave must honour**: a `partial` pack renders the sections that exist WITH a visible "some sections could not be generated - regenerate" affordance. It must never render three of four sections as though the pack were complete - that is the same over-statement class as N8’s disclosure. **Also unresolved here**: `interview_prep_events` has a durable per-attempt log and `prepLog.js` exists (N7) with no specifying document; the read surface is where a "download prep log" control would live, per the standing feature-log directive [[feature-logs]] | A design round (1c) for the pack surface, then the wave that builds it - AFTER N16 and N8 land, since the shape it renders is what they define | `lib/interviewPrep/prepStore.js` `readPrepPack`/`listPrepPacks` - zero non-test callers (canary `fetchApplicationDocs` -> 40+); `app/api/interview-prep/` contains only `route.js` and `route.test.js` - no GET; `lib/interviewPrep/prepTrigger.js:51-74` - fire-and-forget POST, response discarded; [[feature-logs]] - the standing directive the prep log would fall under |
| N26 | **`containsDetectedName` cannot tell a person from a product, and that blocks grounding the prep pack in the candidate’s own résumé.** `prepParse.js:45`’s `NAME_PAIR_RE` matches ANY consecutive Title-Case pair whose second word is outside a 23-entry org-suffix list. Résumé prose is dense with exactly that shape - "React Native", "Apache Kafka", "Google Cloud", "Series B", "Payments Platform". Under the pack contract landed for N16, a name-detected line that carries no citation is REFUSED, and in `aboutYou`/`whyRole` a refusal is a DROP (not a null), so **feeding a résumé in would silently delete most of the candidate’s own material** - the exact opposite of what N8 exists to achieve. The only signal would be the `refusedLines` counter N16 wave A added. **Proven, not predicted**: N16 wave A was asked for a control asserting the line "Shipped the payments service on React Native and Google Cloud." survives intact; it does not, and the agent encoded the AC as vitest `it.fails(...)` rather than widen the heuristic or fabricate a citation. That disclosed failure is live in `prepParse.test.js` today and should be converted to a real assertion by this round. **OWNER RULING, 2026-09-15**: build a **held-out corpus of real résumé prose** and narrow the detector against it, **measuring the false-positive rate** rather than guessing. O-15 stays intact - a real person’s name still requires a cited claim; it is the DETECTOR that is wrong, not the rule. The owner explicitly rejected the alternative of exempting `aboutYou`/`whyRole` from the citation requirement, because that would let an uncited person’s name into those sections. **Do NOT widen `ORG_SUFFIX_WORDS`** - `prepParse.js:48-52` discloses the heuristic as weak by design, and whack-a-mole without a corpus is how ruling R-IP3-63/64’s two-n-gram defect happened. **Corpus discipline** ([[loop-traps-spec]], and backlog N4 records the same trap for the K1-PROHIBITION corpus): the held-out members must be derivable rather than stored beside the implementer’s own module, or a lookup table passes every visible row | A hardening round: build the corpus, measure the current false-positive rate, narrow the detector, re-measure | `lib/interviewPrep/prepParse.js:45` (`NAME_PAIR_RE`), `:48-52` (the heuristic disclosed as weak by design), `:53-77` (the 23-entry org-suffix list); `lib/interviewPrep/prepParse.test.js` - the AC-N8.2 control currently encoded as `it.fails(...)`; backlog N4 - the same held-out-corpus trap, already recorded for K1-PROHIBITION |
| N27 | **Two residuals deferred out of N16 wave C, both about packs that already exist or lines that were silently dropped.** **(a) Legacy packs keep three orphaned top-level keys.** Rows written before N16 stored `tellMeAboutYourself`/`whyThisPosition`/`questionsToAsk` at the TOP level plus a flat `sections.stages` array. Wave C added a read-time limb so the flat stages still resolve, but the three top-level keys are never migrated into the new `sections.{aboutYou,whyRole,askThem}` - they survive as keys nothing will ever read again, and those packs therefore render as missing three of four sections once a read surface exists (N25). Needs a data migration, or a documented decision that legacy rows are regenerated rather than migrated. **(b) `refusedLines` has nowhere to go.** `countRefusedLines` is called once in `route.js` and written to `console.warn`. There is no column for it (`interview_prep_packs`’ contract is `{version, sections, claims}` per the migration’s own comment at `20260914000000_interview_prep.sql:153`, which is why the field was removed from the persisted document). So the ONE signal that the name-detector silently deleted a candidate’s content is a server log line nobody reads. `interview_prep_events` already exists as the durable per-attempt log and is the obvious home. **Why (b) matters more than it looks**: N26 records that `containsDetectedName` drops real résumé and first-person prose, and a checker measured 5 of 10 plausible model-written lines refused. Until the count is durable, there is no way to tell a pack that was generated thin from one that was gutted - and those need opposite responses | A wave after N25 (the read surface) exists, so both residuals can be judged against something the candidate actually sees | `lib/interviewPrep/prepParse.js` `extractStageList` (the read-time limb that recovers flat stages but not the three top-level keys); `app/api/interview-prep/route.js` - `countRefusedLines` result goes to `console.warn`; `supabase/migrations/20260914000000_interview_prep.sql:153` (the `{version, sections, claims}` contract); backlog N26 (the detector), N25 (the read surface) |
| N28 | **A prep-pack stage’s `questions[]` strings are never shape-validated, so a malformed model reply renders as `null`, `5` or `[object Object]` to the candidate.** Executed by a reviewer: `stages:[{name:"S", questions:[null,5,{a:1}], recommendedAnswer:"Real answer."}]` normalizes UNCHANGED and `packStatus` returns `"ready"`. N16 wave D closed this class for the other three sections (`isUsableTextEntry`) and for whole stage ENTRIES (`normalizeStageList`), but not for the strings INSIDE a stage - the one place `normalizeStage` iterates raw model-supplied strings. **Deliberately declined in wave E, with the reason recorded** (a reviewer’s cost call): it needs a malformed model reply to reach at all, it is rendering hygiene rather than an O-15 grounding or CHECK-constraint defect, and fixing it means touching `normalizeStage` - the one function whose behaviour three landed tests and an owner ruling now pin. Small fix, large breakage surface; the right trade was to queue it. **Fix when taken**: in `normalizeStage`’s filter, also drop any `questions[]` entry that is not a non-empty trimmed string - the same rule `isUsableTextEntry` already applies elsewhere. **It is only observable once N25 (the read surface) exists**, which is the natural time to take it | The wave that builds the read surface (N25), or any later round that reopens normalizeStage | `lib/interviewPrep/prepParse.js` `normalizeStage` - iterates `questions[]` without a shape check; `lib/interviewPrep/prepPack.js` `isUsableTextEntry` - the rule already applied to the other three sections; reviewer-executed: a stage with `questions:[null,5,{a:1}]` yields `packStatus === ready` |
| N29 | **The prep pack can only be kicked off automatically; a tracking row needs a manual "prepare me for this interview" control.** Owner request, 2026-09-15. Today generation fires only as a side effect of tailoring - `startInterviewPrepResearch` (`lib/interviewPrep/prepTrigger.js:51`) is called from exactly two places, `app/page.js:1655` (trigger class B3) and `app/hooks/useManualPostings.js:280` (B1) - so a candidate who tailored a posting last week, or who wants a second pass before an interview tomorrow, has no way to ask for one. **The tracking row is the right home and is already the only surface that HAS what the trigger needs**: `app/components/tracking/ApplicationCard.js` renders per-row actions (`:178` JD, `:181` Resume, `:200` Comms, `:222` Edit) against a row carrying a real `application_id` - which is precisely what backlog **N13** records as MISSING at two of the five existing call sites, where `tailorPosting` returns only a `jobId` and the blank-id refusal silently swallows the call. A tracking-row button has no such gap. **Coupled to N25, and the pairing matters**: there is no read surface, so a manual kick-off today would spend a capped attempt and show the candidate nothing at all - no running state, no result, no failure. Build them together, or build N25 first. **A fire-and-forget button with no feedback is worse than no button.** **Design questions**: (a) what the control shows across the four states (never generated / running / ready / failed) - the row already reads pack status via `listPrepPacks`, which is browser-safe by design; (b) what it says when the caps are spent (`attempts >= 6` or `model_calls >= 12`) rather than letting the click fail silently - `claimPrepPack` already returns a distinct `attempts-spent` reason; (c) whether a re-run replaces the stored pack or offers a choice, given `writePrepPackResult` is a plain UPDATE and O-16’s delete control exists separately; (d) the standing [[minimize-clicks]] directive - one action with good defaults, not a menu then a confirm. **Trigger class**: this is a new, candidate-initiated class distinct from B1/B3, and `interview_prep_events.trigger_class` is CHECK-bounded to `("B1", "B3")` (`20260914000000_interview_prep.sql:406-407`), so a manual trigger needs either a migration adding its own value or an explicit ruling to reuse one - **it cannot just be passed through** | A design round settling the four questions and the trigger_class ruling, then the wave that builds it - after or alongside N25 | `lib/interviewPrep/prepTrigger.js:51` (`startInterviewPrepResearch`, the only entry point); `app/page.js:1655` and `app/hooks/useManualPostings.js:280` - the only two call sites today; `app/components/tracking/ApplicationCard.js:178`, `:181`, `:200`, `:222` - the per-row action strip this joins; `supabase/migrations/20260914000000_interview_prep.sql:406-407` - trigger_class CHECK is bounded to B1/B3; coupled to N25 (no read surface) and N13 (the missing application id at two existing sites) |
| N30 | **The committed lockdown migration describes a cleanup block and `anon` revokes that NEVER RAN, and every table in `public` still grants TRUNCATE/REFERENCES/TRIGGER to `anon` and `authenticated`.** Two findings from verifying V2 against the live project, 2026-09-15. **(a) A stale migration file, and the cause is an orchestration error.** `supabase/migrations/20260915000000_interview_prep_spend_lockdown.sql` shipped in `3d9bb27`, and the GitHub Action (`.github/workflows/supabase-migrations.yml`, `supabase db push` on any push touching `supabase/migrations/**`) applied it. Later rounds then CORRECTED THAT FILE IN PLACE and pushed again in `0e83c97` - but `db push` keys on the version stamp, so the edited file was skipped. The committed text now claims a cross-tenant cleanup block, `anon` revokes, and a `raise exception` guard **that are not in the database**. A reader of that file will believe otherwise. This is the exact editing-an-applied-migration hazard the file itself warns about, committed by the orchestrator on a stale premise. **What IS live, verified by query**: `authenticated` holds SELECT only on `interview_prep_spend` (N12 closed); both functions are `prosecdef = true` with `search_path` pinned empty; `proacl` carries no `=X/` entry, so PUBLIC has no execute and `anon` none either; and **zero cross-tenant rows exist**, so the cleanup block was always a no-op on this project. **(b) Schema-wide least privilege, NOT a vulnerability.** Every table in `public` grants `TRUNCATE`, `REFERENCES` and `TRIGGER` to both `anon` and `authenticated` - `applications`, `generated_resumes`, `experience_pages`, all of them. This is Supabase’s DEFAULT posture, not something a migration here granted; the migrations revoked DML and never touched these. **No known path reaches them**: `anon`/`authenticated` are not login roles (PostgREST connects as `authenticator` and SET ROLEs), PostgREST exposes no TRUNCATE, and REFERENCES/TRIGGER need DDL. TRUNCATE would bypass RLS if it were reachable, which is why it is worth removing on principle. **Do NOT fix (b) with a one-table revoke** - that was the unapplied correction, and doing it for `interview_prep_spend` alone is inconsistent and buys nothing. **REFERENCES in particular may be load-bearing** for any hand-added foreign key, so this needs a design round and a `pg_constraint` survey before anything is revoked across 20+ tables | (a) a new migration with a LATER timestamp carrying whatever of the corrections is still wanted - never another edit to 20260915000000; (b) a design round for schema-wide least privilege, with a survey first | `.github/workflows/supabase-migrations.yml` - `supabase db push` on push to main touching migrations; `supabase/migrations/20260915000000_interview_prep_spend_lockdown.sql` - committed text diverges from what applied; live queries 2026-09-15: `role_table_grants` (authenticated = SELECT only on spend; TRUNCATE/REFERENCES/TRIGGER on every public table for anon+authenticated), `pg_proc.prosecdef`/`proconfig`/`proacl`, and a zero-row cross-tenant join |

## Owner decisions outstanding

| # | Question | Why it is blocked |
|---|---|---|
| D2 | **The transport claim is doc-verified, not wire-verified.** | No `GEMINI_API_KEY` in this checkout. Recorded in the research ledger with that caveat |

## Verification owed

| # | Item | Measured how |
|---|---|---|
| V1 | **Rich résumé copy has never touched a real clipboard.** The unit tests prove both `text/html` and `text/plain` are set with the right payloads, each with a negative control — but nobody has pasted into Word or Google Docs. | `npx vitest run lib/clipboard app/components/preview app/components/DocumentPreviewDialog.copy.test.js` → 339/339. A real paste is the missing instrument |

## Standing hazards — re-read before trusting a number

- **An instrument here is defective until a constructed mutant kills it.** The author's own claim that a mutant
  dies is not evidence: one wave reported a kill in good faith and the real mutant survived, because the mutant
  tested was easier than the named one (ledger `T3-S9-3`).
- **A count beside a list it does not match** has occurred three times — a table heading, a column enumeration,
  and a ledger figure asserted as 4 where `grep -c "module:"` returns **5**.
- **A claim restated from another document is not verified.** Six false claims propagated here purely by
  restatement, each one grep-checkable. Run the grep with a canary before repeating a fact.
- **Ruling on a check's blockers alone silently drops its majors.** Nine majors once sat unrouted through three
  revisions because a ruling answered only the blockers, and everything downstream read the check as handled.
