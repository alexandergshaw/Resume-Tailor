### R-313 | area: sticky-question-strip | parallel-safe: yes | automatable: partly

**Summary:** The current-question panel, relocated out of `CopilotDashboard` into a sticky strip mounted once per client (`app/copilot/dashboard/StickyQuestionStrip.js`), actually stays pinned directly below `AppHeader` at any scroll offset without ever occluding it, its height cap resolves the design's `max()`/percentage arithmetic to real pixel values and scrolls internally rather than clipping the question when content exceeds it, and below a hosting threshold it falls back to a plain in-flow block with no cap and nothing clipped. These are geometric and rendering facts jsdom's absent layout engine cannot observe -- `stickyQuestionGuards.test.js`, `useStickyTop.test.js` and `StickyQuestionStrip.test.js` each say so in their own file headers and correctly decline to assert them, which means without this entry none of the five is checked by anyone, ever, after the feature ships.

**Steps:**
1. From `hello-world`, run `npx vitest run --no-file-parallelism app/copilot/stickyQuestionGuards.test.js app/copilot/useStickyTop.test.js app/copilot/dashboard/StickyQuestionStrip.test.js app/copilot/copilotHeadingOrder.test.js app/copilot/CopilotClient.structure.test.js`.
2. Read `useStickyTop.js`'s `band()` / `isHostable()` pair (`app/copilot/useStickyTop.js:24-85`) and `StickyQuestionStrip.js`'s `stickyTop == null` branches (`app/copilot/dashboard/StickyQuestionStrip.js:49-88`) against the five properties in steps 3-7 -- the three files run in step 1 each state in their own header that none of those five is assertable in jsdom.
3. In a real browser, live mode, scroll to `scrollY = 1500` and read `[data-app-header]`'s and the strip's own outer element's `getBoundingClientRect()`. Confirm the header's rect is `{top: 0, bottom: 61}` and the strip's rect starts at `top: 61` -- exactly the header's bottom -- and confirm `document.elementFromPoint(30, 30)` returns a node inside `[data-app-header]`, not the strip: the strip paints below the header (`zIndex: 2` against the header's `1100`), never over it.
4. At an inner viewport height of 422 CSS px (root font 16px), read the strip's inner scroll box's computed `max-height`. Expect `162.45px` -- `0.45 × (422 − 61)`, the `@media (max-height: 519px)` band. At 760 CSS px, expect `209.7px` -- `0.30 × (760 − 61)`, the base band.
5. With a question long enough to exceed the cap (`HeldQuestionPanel`'s held state, or any question over ~250 characters), confirm the inner scroll box's `clientHeight` (162) is less than its `scrollHeight` (412) -- it scrolls internally; no question text is clipped or lost.
6. Narrow the browser window to a 280 CSS px tall inner viewport. Confirm the strip's outer element computes `position: static` (not `sticky`), its inner box computes `max-height: none` with `overflow-y: visible`, and `scrollHeight === clientHeight` on that box (measured: both `244`) -- the panel renders at its natural height with nothing clipped, exactly as it did before this feature existed.
7. Scroll a real session on a phone whose browser URL bar collapses and expands during the scroll (or emulate one in devtools). Confirm the strip does not flip between pinned/capped and static/uncapped mid-scroll -- it can only change on an actual resize, because the hosting predicate reads `document.documentElement.clientHeight` (the LARGE, static viewport) rather than `window.innerHeight` (the DYNAMIC one a URL bar moves by 50-100 CSS px while the page scrolls). That source-level fact is already pinned by G-5 in step 1; step 7 confirms the reason it is pinned actually holds in a real gesture.

**Expected:** All 45 tests across the five files in step 1 pass. None of them assert steps 3-7's five properties -- each file's own header explains why: jsdom has no layout engine, so `getBoundingClientRect()` returns zeroes, `max()`/`calc()`/`dvh` never resolve, `@media (max-height:)` never evaluates, and nothing is ever painted. Steps 3-7 are the only check that ever exercises them, and after this entry is the only place telling anyone to run them. `useStickyTop.test.js` gets partway there by stubbing `document.documentElement.clientHeight` and the header's `getBoundingClientRect()` directly -- proving the HOOK's own state machine (returns the header's height when hostable, `null` when not, republishes on resize, cleans up on unmount) -- but a stubbed rect is not a resolved `max()`, a painted `position: sticky`, or a browser's own URL-bar-driven viewport change, which is exactly the gap steps 3-7 close. One property of this relocation IS fully automated and needs no browser: that the question panel renders exactly ONCE, never duplicated between the strip and `CopilotDashboard`. `copilotHeadingOrder.test.js`'s *"renders the current-question title exactly once as a heading"* and `CopilotClient.structure.test.js`'s *"dashboard/CopilotDashboard.js no longer calls <CurrentQuestionPanel"* (a hard `indexOf(...) === -1` on the dashboard's own source) together prove it, and both already run in step 1 -- a reader auditing "does the relocation duplicate the question" should be pointed at those two assertions, not sent to a browser.

### R-314 | area: sticky-question-strip | parallel-safe: yes | automatable: yes

**Summary:** The sticky strip's five cap constants (`STRIP_FLOOR_PX`, `STRIP_FLOOR_REM`, `STRIP_GUTTER_PX`, `STRIP_BANDS`, `STRIP_MAX_SHARE`, all in `app/copilot/useStickyTop.js`) stay MODULE-LOCAL rather than exported, deliberately -- exporting any of them would mint a name whose only outside consumer is a test, which moves `lib/sourceScan/exportReachability.sweep.test.js`'s own `:476` and `:491` counts, the two figures that file's own comments call "never a number to bump."

**Steps:**
1. From `hello-world`, run `npx vitest run --no-file-parallelism app/copilot/stickyQuestionGuards.test.js lib/sourceScan/exportReachability.sweep.test.js`.
2. Read `stickyQuestionGuards.test.js`'s G-5 (*"keeps the five cap constants module-local, and the predicate reads the same literals"*) against `useStickyTop.js`'s own module comment (`:11-23`) explaining why, and `exportReachability.sweep.test.js:486-490`'s comment recording the identical precedent for `MAX_ACTIVITY_FIELD_CHARS`/`truncateField` -- "caught by this very assertion on their first run and un-exported, because both are applied inside that module and read nowhere else" -- immediately above the `:491` assertion these five constants are pinned against.

**Expected:** All 57 tests across the two files pass. `useStickyTop.js` declares `STRIP_FLOOR_PX`, `STRIP_FLOOR_REM`, `STRIP_GUTTER_PX`, `STRIP_BANDS` and `STRIP_MAX_SHARE` each exactly once, as a module-local `const`, exports none of them, and re-exports none of them -- only `band` and `useStickyTop` are exported, and `StickyQuestionStrip.js` is `band`'s sole shipping importer. `exportReachability.sweep.test.js:476` (`TEST_REFERENCED.length`) reads `303` and `:491` (`UNUSED_IN_SHIPPING_MODULES.length`) reads `359`, both unchanged by this feature having landed at all. **Exporting any of the five would move both.** Each would become `unused-export` + `importedByATest` -- referenced only by `stickyQuestionGuards.test.js`, never by shipping code -- landing it in `TEST_REFERENCED` and pushing `:476` toward 308 and `:491` toward 364 depending on how many of the five are exported. This is not a hygiene nit on its own: G-5 already asserts the cap's emitted CSS and the hosting predicate are built from the SAME literals, precisely because two copies of `4.25` -- one exported for a test to read directly, one still inlined in the predicate -- is exactly the drift that produces a cap and a predicate that silently disagree. The module-local ruling and the reachability-sweep hygiene rule are protecting the same property from two different directions; this case exists so that a future "just export it, the test will read it directly" change is caught by a count moving in a file the diff did not otherwise touch, rather than by nobody at all.

### R-320 | area: sticky-question-strip | parallel-safe: yes | automatable: yes

**Summary:** Adding the speaking-stats row changed the question's own height cap by **nothing at all**. `band()` and the hosting predicate it agrees with (`FLOOR`, `avail`, `isHostable`, and the five `STRIP_*` constants, all in `app/copilot/useStickyTop.js`) are **byte-identical to `93ad8f7`**, the last commit before the row existed, and the row is a SIBLING of the capped box rather than a debit against it. This case exists in its own right because it is a property of ABSENCE: no other case in this document states it, and a change that quietly shaves the cap to "make room" for the row would break the question's first-line guarantee on short viewports while every stats test stayed green.

**Steps:**
1. From `hello-world`, run `npx vitest run --no-file-parallelism app/copilot/stickyQuestionGuards.test.js app/copilot/dashboard/StickyQuestionStrip.test.js`.
2. From the repository root, extract the cap's arithmetic from both revisions and compare it byte for byte (any scratch path works for the first line):
   ```
   git show 93ad8f7:hello-world/app/copilot/useStickyTop.js > ust_head.js
   ext() { sed -n '/^const STRIP_FLOOR_PX/,/^const STRIP_MAX_SHARE/p' "$1"
           sed -n '/^const FLOOR = /,/^}$/p' "$1"; }
   diff <(ext ust_head.js | tr -d '\r') \
        <(ext hello-world/app/copilot/useStickyTop.js | tr -d '\r')
   ext hello-world/app/copilot/useStickyTop.js | tr -d '\r' | md5sum
   ```
   The first range is the five `STRIP_*` constants; the second runs from `FLOOR` through the closing brace of `isHostable`, taking `avail` and `band` with it. The three `STATS_*` constants this feature added sit BETWEEN the two ranges, so they are excluded without having to be named.
3. Read `StickyQuestionStrip.js` and confirm `<StatsRow …/>` is a direct child of the outer sticky Box and a sibling AFTER the capped box -- never inside the `overflowY: auto` container, where it could be scrolled away with the question.

**Expected:** All 44 tests across the two files pass, including G-5's *"band() emits the exact 519 / 699 / base strings, in cascade order, for both units"* and *"AC 1 / AC 3 -- the row is a SIBLING of the capped box, and follows it in DOM order"*. Step 2's `diff` prints nothing and the md5 reads `baea7342d6f425c6f09b8fa373e98a53` on both revisions. In a real browser at 375x812 with a 51px app header the capped box computes `max-height: 228.3px` **with the row present and with it absent**, and its own height is identical in both (measured 123.97px both ways for the same question) -- `max(calc(64px + 4.25rem), calc((812-51) * 0.30)) = max(132, 228.3) = 228.3px`, exactly what `band()` emits.

**Verified by mutation.** Perturbing the base band's percentage by one hundredth -- `0.30` to `0.29`, the smallest change that could plausibly be described as "making room for the row" -- is caught immediately and by its emitted string, not by a paraphrase of it:

```
FAIL ... > G-5: the cap and the hosting predicate cannot drift apart
          > band() emits the exact 519 / 699 / base strings, in cascade order, for both units
Expected: "max(calc(64px + 4.25rem), calc((100dvh - var(--sticky-top, 0px)) * 0.30))"
Received: "max(calc(64px + 4.25rem), calc((100dvh - var(--sticky-top, 0px)) * 0.29))"
Tests  1 failed | 7 passed (8)
```

**Why byte-identity and not "the tests still pass".** G-5 pins the three emitted strings and the constants they are built from, but it cannot see a change to `isHostable`'s inequality, to the band table's ORDER, or to `avail()`'s fallback that happens to preserve those three strings. The md5 in step 2 covers the whole block as one artefact, which is the only form in which "this feature changed the question's cap by nothing" is actually checkable. If the anchor commit is ever rewritten, re-anchor step 2 on the last commit before `StatsRow.js` was added (`git log --diff-filter=A -- hello-world/app/copilot/dashboard/StatsRow.js`), not on a newer one.

