### R-308 | area: tracking-a11y | parallel-safe: yes | automatable: yes

**Summary:** The per-row "Download tailored .docx" affordance in the Tracking tab's desktop (>=900px) table branch is a real MUI `IconButton`, not the hand-rolled `<span role="button" tabIndex={0} onClick>` with no key handler it used to be -- so Enter and Space activate it -- and it still downloads on activation and still drags a resolved `.docx` straight into an ATS upload field.

**Steps:**
1. From `hello-world`, run `npx vitest run --no-file-parallelism app/components/TrackingTab.keyboard.test.js`.
2. Read `renderDownloadControl` in `app/components/TrackingTab.js` and confirm its only call site is inside the `<TableBody>` of the non-compact branch, below the `</TableHead>` the compact branch replaces.
3. In a real browser at >=900px with a `.docx` source résumé uploaded and at least one tailored résumé stored: Tab to a row's download control, press **Enter** -- the `.docx` downloads. Tab to it again, press **Space** -- it downloads again. Then drag it with the mouse into a file-upload field on a real job posting and confirm the dropped file is the tailored `.docx`, correctly named.

**Expected:** All 13 tests pass. Every control in the table branch whose accessible name matches `/download/i` has `tagName === "BUTTON"`, carries `.MuiButtonBase-root` (so it inherits the R-305 focus ring), and carries no hand-rolled `role="button"` anywhere in the file. Activating it calls `downloadDocxFiles` with the row's `company` and `docx_path`. The control still carries `draggable="true"` and its `onDragStart`, which resolves the blob through `resolveDocumentBlob` and names the file with `getDownloadFileNameForTitle` -- MUI's button roots carry `draggable` fine, so making the control accessible cost the drag route nothing. The <900px card branch's own real `<Button>Download</Button>` is untouched and separately asserted.

**The tooltip must keep naming the drag.** Before this change the tooltip read "Download or drag to upload tailored DOCX"; a first pass at the fix reduced it to "Download tailored .docx", which left a working feature with no label anywhere in the UI that says it can be dragged. The available-case title is now "Download or drag to upload tailored .docx" and is used for BOTH the `Tooltip` and the `aria-label`. If a future edit shortens it, the drag affordance becomes undiscoverable again while every test above stays green -- no assertion pins the word "drag" in that string.

### R-309 | area: tracking-a11y | parallel-safe: yes | automatable: yes

**Summary:** When the source résumé is missing or is not a `.docx`, the Tracking tab's desktop download control announces that unavailability through `aria-disabled` and **stays in the tab order**, so the `aria-label` that explains *why* it is unavailable is reachable by the keyboard and screen-reader users who need it -- rather than through the `disabled` attribute, which sets `tabindex="-1"` and puts the explanation out of reach of exactly those users.

**Steps:**
1. From `hello-world`, run `npx vitest run --no-file-parallelism app/components/TrackingTab.keyboard.test.js`.
2. Read the `aria-disabled` / `sx` pair inside `renderDownloadControl` in `app/components/TrackingTab.js` beside the rule it follows, written out in `app/components/ChatPanel.js` above the Send button: the `disabled` attribute "was only ever a correctness no-op that cost the control its focusability".
3. In a real browser at >=900px with **no** résumé uploaded (and again with a non-`.docx` résumé uploaded): Tab to the download control. Confirm it receives focus and paints the R-305 ring, confirm a screen reader announces it as dimmed/unavailable AND reads the reason ("Upload your source resume (.docx) to enable downloads." / "Source resume must be a .docx file to download."), and confirm pressing Enter downloads nothing.

**Expected:** All 13 tests pass. With `isDocxResume` forced false, every download control reports `node.disabled === false` and no `tabindex="-1"`, while `getAttribute("aria-disabled") === "true"`. Because the control is found by an accessible name matching `/download/i` and that name IS the explanation string, a control that lost its `aria-label` would not be found at all and the case would fail on the `controls.length` assertion. Activating it dispatches no download: the `onClick` and `onDragStart` bodies both return early on `downloadUnavailable`, so `aria-disabled` costs nothing functionally, and the `opacity: 0.5` / `pointerEvents: "none"` styling stands in for the `.Mui-disabled` dimming the attribute would have supplied.

**This assertion is order-dependent inside its own file, and that is deliberate.** The sibling case *"stays disabled-but-explained when there is no .docx source resume"* accepts EITHER `disabled` or `aria-disabled` for the programmatic-state half, and only runs its "activating it runs no download" half inside an `if (aria-disabled === "true")` guard. Switching the source back to the `disabled` attribute therefore leaves that case green **and silently skips half of what it claims to check** -- the separate `keeps the unavailable download reachable by keyboard` case above is the only thing standing between this control and that regression. Do not merge the two.

### R-310 | area: tracking-a11y | parallel-safe: yes | automatable: partly

**Summary:** Below 900px the Tracking tab replaces its whole `<TableHead>` -- and with it all four `TableSortLabel`s -- with a card layout, which left sorting unreachable at that width on a laptop with devtools open, not just on a phone. The card branch now renders a native `<select>` "Sort by" covering all four sortable fields in both directions **plus a working "Default order" that resets the sort**, with its floating label forced to `shrink` so it does not render on top of the value.

**Steps:**
1. From `hello-world`, run `npx vitest run --no-file-parallelism app/components/TrackingTab.keyboard.test.js`.
2. Read `SORT_FIELD_OPTIONS` at module scope in `app/components/TrackingTab.js` and the `<TextField select>` that consumes it, and confirm `setInterviewSort` is threaded from `app/page.js` beside the pre-existing `toggleInterviewSort`.
3. In a real browser narrowed to below 900px: Tab to the "Sort by" control, open it with the keyboard, and step through all nine options. Confirm the visible list order changes for each of the eight field/direction pairs, confirm choosing **Default order** puts the list back into fetch order, and confirm the words "Sort by" sit ABOVE the field rather than on top of the visible "Default order" text at rest.

**Expected:** All 13 tests pass. The control is a native `<select>` (not MUI's non-native `Select`, which would break WCAG 2.5.3 the way this repo has already recorded), is in the tab order, and is not disabled. Its options cover company, role/title, status and applied date in both directions. The assertions are stated on the RESULTING sort state, not on the wiring: the component's calls are replayed through the real reducer from `app/page.js`, so choosing the applied-date option must yield `field: "applied_at"`, and choosing **Default order** from a sorted state must yield `{ field: null, dir: "asc" }` -- a setter call that fires but does not actually clear the sort still fails. The four desktop `TableSortLabel`s are untouched and separately asserted.

**"Default order" and the label are two independently reachable defects with two independent guards.** `SORT_FIELD_OPTIONS[0]` is a real option, not a placeholder: without its own branch in the `onChange` handler it renders, is selectable, and reaches no setter at all, leaving the compact layout with **no route back to fetch order** once a user has sorted -- the desktop 3-state cycle has one (a third click on the same `TableSortLabel`), the card layout does not. Separately, `slotProps={{ ..., inputLabel: { shrink: true } }}` is required **unconditionally** here for the reason `app/copilot/MicPicker.js`'s own comment gives: MUI computes `shrink` from `isFilled(value)`, the control's value is `""` at rest, and a native `<select>` has no genuinely blank state for the label to sit on top of -- "Default order" is always showing. `app/copilot/roles/RolePicker.js` omits `shrink` only because its value is never empty, so that precedent does not transfer. The `data-shrink` assertion is the machine-checkable stand-in; **jsdom has no layout engine, so only step 3 can see the overlap itself.**

